/**
 * Add command - Generate a new page with controller
 */
export declare function addCommand(pageName?: string, routePath?: string, options?: {
    mvc?: boolean;
    project?: string;
}): Promise<void>;
