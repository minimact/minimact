/**
 * @minimact/core/playground
 *
 * Playground Bridge for Minimact Swig IDE integration
 *
 * Usage:
 *   import { PlaygroundBridge } from '@minimact/core/playground';
 *
 * Bundle impact: +0.5 KB gzipped
 *
 * This module provides real-time metrics and cache hit visualization
 * for the Minimact Swig IDE. Only import when using the IDE.
 */

export { PlaygroundBridge } from './playground-bridge';
